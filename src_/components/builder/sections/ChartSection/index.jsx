import { Box } from "@mui/material";

const ImageSection = ({ src, alt, height, objectFit, borderRadius }) => {
    return (
        <>Aqui va a ir el grafico</>
    );
};

export default ImageSection;